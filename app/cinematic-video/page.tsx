import { redirect } from "next/navigation";

export const metadata = {
  title: "LumenTech Africa Cinematic Video",
  description: "A 20-second vertical cinematic brand video for LumenTech Africa."
};

export default function CinematicVideoPage() {
  redirect("/");
}
